/* MOSAICO — Solo Lab: bots identificados por perfil, com custódia única e estado público em tempo real. */
(function(){
if(new URLSearchParams(location.search).get('soloLab')!=='1')return;
const defs=[['Bot Analítico','analítico',[2,4],'#55b8ff'],['Bot Cauteloso','cauteloso',[1,3],'#70d6a0'],['Bot Intuitivo','intuitivo',[3,5],'#c792ea'],['Bot Agressivo','impulsivo',[5,8],'#ff7b72'],['Bot Documental','documental',[2,4],'#efc878'],['Bot Explorador','explorador',[3,6],'#63d5da'],['Bot Revisionista','revisionista',[1,3],'#f39ac7']],BOTS=defs.map((x,i)=>({slot:i+2,name:x[0],profile:x[1],decision:x[2],color:x[3],coins:12,hand:[],burnedFields:new Set(),actions:0,hypothesis:'Em análise',lastAction:'Aguardando a vez'})),LAB=window.MosaicoSoloBots={active:true,bots:BOTS,events:[],log(type,data={}){this.events.push({at:Date.now(),type,...data})}};
let activeSlot=0,armed=false,acted=false,startSeconds=0,targetLeft=0,lastLeft=null,serial=0,dealt=false;
const slotFrom=s=>{const m=/^Arquivo\s+(\d+)$/i.exec(s||'');return m?+m[1]:0},seconds=()=>{const m=/(\d+):(\d+)/.exec(document.getElementById('turnTimer')?.textContent||'');return m?(+m[1]*60)+(+m[2]):0},pick=a=>a[Math.floor(Math.random()*a.length)],botFor=s=>BOTS.find(b=>b.slot===s);
function sync(){const snap=window.MosaicoCore?.getSnapshot();if(!snap)return;BOTS.forEach(b=>{const o=snap.opponents[b.slot-2];if(o)b.hand=[...o.hand]})}function deal(){if(dealt||!window.MosaicoCore)return;const core=window.MosaicoCore;BOTS.forEach(b=>{const id=core.getSnapshot().pool[0];if(id)core.solo.deal(b.slot,id)});sync();dealt=true;BOTS.forEach(update);LAB.log('custody-initialized',{hands:BOTS.map(b=>({bot:b.name,hand:[...b.hand]})),pool:core.getSnapshot().pool})}function update(b){const c=[...document.querySelectorAll('#opponents .opponent')][b.slot-2];if(!c)return;c.style.setProperty('--participant-color',b.color);c.dataset.slot=b.slot;c.innerHTML=`<b class="participant-name">${b.name}</b><span class="participant-fragments">${b.hand.length} ${b.hand.length===1?'fragmento':'fragmentos'}</span><span class="participant-state">${b.hypothesis}</span>`}function strength(b){const key=new Set(['F10','F11','F13','F14','F15']),strong=b.hand.filter(x=>key.has(x)).length;return Math.min(.18,strong*.035)+Math.min(.08,Math.max(0,b.hand.length-1)*.02)}function choose(b){const r=Math.random();if((b.profile==='analítico'||b.profile==='documental')&&b.coins>=4&&r<.55)return'buy';if(b.profile==='cauteloso')return b.coins>=4&&r<.65?'buy':'observe';if(b.profile==='impulsivo')return b.coins>=3&&r<.7?'risk':b.coins>=2?'capture':'observe';if(b.profile==='explorador')return b.coins>=2&&r<.55?'capture':b.coins>=4?'buy':'observe';if(b.profile==='revisionista')return b.actions>1&&b.coins>=3?'risk':b.coins>=4?'buy':'observe';if(b.profile==='intuitivo')return b.coins>=3&&r<.55?'risk':b.coins>=4?'buy':'observe';return b.coins>=3?'risk':'observe'}function act(b,elapsed){const core=window.MosaicoCore,snap=core?.getSnapshot(),kind=choose(b);let detail=null,label='observou';b.hypothesis='Revisando evidências';if(kind==='buy'&&b.coins>=4){const id=core.solo.buy(b.slot);if(id){b.coins-=4;label=`comprou ${id}`;b.hypothesis='Nova evidência incorporada'}}else if(kind==='capture'&&b.coins>=2){sync();const targets=BOTS.filter(x=>x.slot!==b.slot&&x.hand.length),t=pick(targets);if(t){const id=pick(t.hand);if(core.solo.capture(b.slot,t.slot,id)){b.coins-=2;label=`capturou ${id} de ${t.name}`;b.hypothesis='Custódia atualizada';t.hypothesis='Fragmento transferido';detail={id,from:t.name}}}}else if(kind==='risk'&&b.coins>=3&&snap){const open=snap.answers.map((_,i)=>i).filter(i=>!snap.locked.includes(i)&&!b.burnedFields.has(i));if(open.length){const field=pick(open),base={analítico:.62,documental:.60,cauteloso:.55,revisionista:.58,intuitivo:.48,explorador:.46,impulsivo:.36}[b.profile]||.45,skill=Math.min(.92,base+strength(b)),correct=Math.random()<skill,result=core.solo.risk(b.slot,field,correct);b.coins-=3;if(result==='locked'){b.coins+=3;b.hypothesis=`Campo ${field+1} confirmado`}else{b.burnedFields.add(field);b.hypothesis=`Revisando campo ${field+1}`}detail={field,result,skill:Number(skill.toFixed(2))};label=result==='locked'?`travou campo ${field+1}`:`errou campo ${field+1}`}}else b.hypothesis='Aguardando nova evidência';sync();b.lastAction=label;b.actions++;BOTS.forEach(update);LAB.log('bot-action',{serial,slot:b.slot,bot:b.name,action:label,detail,hand:[...b.hand],burned:[...b.burnedFields],elapsed,handSeconds:startSeconds,secondsRemaining:targetLeft});core.solo.finishTurn(`${b.name}: ${label}.`)}function arm(b,left){activeSlot=b.slot;armed=true;acted=false;startSeconds=left;lastLeft=left;serial++;const[a,z]=b.decision;targetLeft=a+Math.floor(Math.random()*(z-a+1));targetLeft=Math.max(1,Math.min(left-1,targetLeft));b.hypothesis='Analisando a jogada';BOTS.forEach(update);LAB.log('bot-thinking',{serial,bot:b.name,handSeconds:left,actsWithSecondsRemaining:targetLeft})}
function tick(){const table=document.querySelector('[data-screen="table"]');if(!table?.classList.contains('active')){activeSlot=0;armed=false;acted=false;lastLeft=null;const hud=document.getElementById('hudTurn');if(hud)hud.textContent='—';return}deal();sync();BOTS.forEach(update);const turn=document.getElementById('turnPlayer')?.textContent.trim()||'',slot=slotFrom(turn),left=seconds(),hud=document.getElementById('hudTurn');if(slot===1){activeSlot=1;armed=false;acted=false;lastLeft=left;if(hud)hud.textContent='Você';return}const b=botFor(slot);if(!b)return;if(hud)hud.textContent=b.name;const valid=left===30||left===60,reset=lastLeft!==null&&left>lastLeft;if(slot!==activeSlot){activeSlot=slot;armed=false;acted=false;lastLeft=left}if(!armed&&(valid||reset))arm(b,left>=46?60:30);if(!armed){lastLeft=left;return}if(!acted&&left>0&&left<=targetLeft){acted=true;act(b,Math.max(0,startSeconds-left))}if(!acted&&left===0){acted=true;b.hypothesis='Tempo esgotado';BOTS.forEach(update);LAB.log('bot-timeout',{serial,slot:b.slot,bot:b.name,handSeconds:startSeconds})}lastLeft=left}
setInterval(tick,120);window.addEventListener('pagehide',()=>LAB.active=false);
})();